# 麻雀・ポーカー成績記録 — 引き継ぎメモ

このメモは `index.html` の実装を実際に読んで作成した。推測で書いた箇所はなく、
分からなかった点は「要確認」と明記している。

---

## 1. 全体構成

- 単一の `index.html` ファイルにHTML/CSS/JSがすべて収まっている構成（外部JSファイルなし、フレームワークなし、ビルド工程なし）。
- データはすべてブラウザの **localStorage** に保存される（サーバー・DBなし）。
- 公開はGitHub Pages。ローカルは `git` 管理下にあり、GitHubリポジトリ
  `https://github.com/worldmahjongunion-sys/mahjong-poker-record`（Public）と連動している。
  公開URL: `https://worldmahjongunion-sys.github.io/mahjong-poker-record/`
- `index.html` の `<head>` に `<link rel="manifest" href="manifest.json">` があり、PWA化を意図した記述がある（`<meta name="theme-color" content="#0d4a30">` も設定済み）。
  `manifest.json` / `icon-192.png` / `icon-512.png` はこのフォルダにもローカルのgit履歴にも存在しないが、これはGitHubのWeb UIから直接アップロードして追加したためで正常な状態。ローカルには無いのが正しい。

---

## 2. 主要ファイル

| ファイル | 状態・役割 |
|---|---|
| `index.html` | アプリ本体。HTML/CSS/JS一式（1655行）。 |
| `manifest.json` | GitHubのWeb UIから直接アップロードしたため、GitHub側にのみ存在する（ローカル未取得）。 |
| `icon-192.png` / `icon-512.png` | 同上。GitHub側にのみ存在する。 |
| `仕様書.md` | 最初に決めた仕様。麻雀／ポーカーのタブ・入力フォーム・一覧・簡易集計のみを想定した初版。現状の実装（メンバー管理・順位自動判定・会場・相性判定など）はこれより大幅に進んでいる。 |
| `基準.md` | プロジェクトの正本。最終更新2026-08-04時点の記述だが、末尾に2026-08-12付「対応中の修正リスト」（後述）があり、そのリストの項目は現index.htmlに反映済み（type="text" inputmode="decimal"化・±ボタン・サニタイズ・NaN→0フォールバックなど、コード上で確認できた）。ただしチェックボックスは未更新（`[ ]`のまま）。 |
| `old_0803_使わない.html` | 冒頭のCSS（`:root`変数・タブ構成など）を見る限り旧バージョンの全体コピー。ファイル名の通り「使わない」＝現行では不使用の過去バックアップ。 |
| `index_backup_20260812.html` / `index_backup_20260814.html` | 日付付きバックアップ。`git status` では未追跡（untracked）。 |

---

## 3. localStorageのキーとデータ形

4つのキーに分けて配列を保存している（`STORAGE_KEY_*` 定数、index.html:596-599）。

### `mahjongPokerRecord_mahjong`（麻雀記録）
```
{
  id: number,            // generateId()で発行。常に増加するユニークID
  date: string,           // "YYYY-MM-DD"
  event: string,          // 大会名（最大100文字）
  round: number,          // 回戦番号（整数、1〜999）
  rank: number,           // 自動算出された順位（1〜4）
  point: number,          // 自分の最終ポイント（小数可、-9999〜9999）
  opponents: number[],    // 対戦相手3人のメンバーid
  opponentPoints: number[], // 対戦相手3人の最終ポイント（opponentsと同順）
  venueId: number | '',   // 会場id。未設定は空文字
  tourType: string,       // 大会種別（リーグ戦／ワンデー／セット／オンライン／空）
  memo: string            // 最大500文字
}
```

### `mahjongPokerRecord_poker`（ポーカー記録）
```
{
  id: number,
  date: string,           // "YYYY-MM-DD"
  event: string,          // 最大100文字
  players: number,        // 参加人数（整数、1〜9999）
  rank: number,            // 順位（整数、1〜9999。players以下であることを検証）
  prize: string,           // プライズ/得点（自由記述テキスト、最大100文字）
  memo: string,             // 最大500文字
  venueId: number | '',
  format: string,          // MTT／SNG／リング／空
  buyin: number | ''       // バイイン（0以上、空欄可）
}
```

### `mahjongPokerRecord_members`（メンバー）
```
{ id: number, name: string }  // nameは最大50文字
```

### `mahjongPokerRecord_venues`（会場。麻雀・ポーカー共通）
```
{ id: number, name: string }  // nameは最大50文字
```

---

## 4. 主要関数（役割ごとの一覧）

### 保存／読込
- `loadRecords(key, label)` — localStorageから読み込み、`JSON.parse`失敗や配列でない場合は空配列を返しつつ `loadErrorLabels` に記録（起動時に画面上部の通知に使う）。
- `saveRecords(key, records)` — 保存を試み成功/失敗をbooleanで返す。`QuotaExceededError`時は専用メッセージ、それ以外は汎用メッセージを `showGlobalNotice` で表示。
- `saveMembers()` / `saveVenues()` — 上記の薄いラッパー。
- `showGlobalNotice(msg)` / `hideGlobalNotice()` — 画面上部の赤い通知バーの表示/非表示。

### 起動時マイグレーション（`sanitizeLoadedData()` → `migrateDuplicateMemberIds()` → `migrateRecordFields()` の順に呼ばれる。index.html末尾1645-1647行目）
- `sanitizeLoadedData()` — 読み込んだmembers/venues/mjRecords/pkRecordsの各フィールドの型・欠損を強制的に補正する（値の意味は変えない）。`toSafeString` / `toSafeId` / `toSafeNumber` / `toSafeInt` を使用。
- `migrateDuplicateMemberIds()` — `Date.now()`ベースの旧ID採番で同一ミリ秒衝突により重複していたメンバーIDを振り直す。麻雀記録の`opponents`配列内の旧IDも、重複グループの先頭メンバーの新IDに寄せ替える。
- `migrateRecordFields()` — 会場・大会種別・形式・バイインを後から追加した際、それ以前の記録に該当フィールドが無いレコードにだけ空文字を補う（既存値は変更しない）。

### 順位判定・スコア計算（麻雀）
- `getCurrentScores()` — 入力フォームの現在値（自分＋対戦相手3人）を検証し、同点は同順位（次順位を飛ばす方式）で順位を算出。未入力・人数不足・範囲外はreadyをfalseにしてメッセージを返す。
- `updateRankPreview()` — フォーム入力のたびに `getCurrentScores()` を呼び、順位プレビューと警告文を更新。`updatePointSum()`も同時に呼ぶ。
- `updatePointSum()` — 自分＋選択済み対戦相手の入力済み分だけをリアルタイム合計表示（4人揃っていなくても部分合計を出す）。合計が0でない場合は警告色＋注記表示。
- `computeMahjongCompat()` — 相性判定。同卓10回戦以上（`COMPAT_MIN_MATCHES`）のメンバーを対象にし、対象人数が10人未満（`COMPAT_MIN_CANDIDATES`）なら何も判定しない。平均順位2.5未満を「好相性」上位5人（`COMPAT_TOP_N`）、2.5より大きいものを「苦手」上位5人として返す。ちょうど2.5はどちらにも含めない。

### 検索・並び替え
- `mjRecordMatches(r, q)` / `pkRecordMatches(r, q)` — 大会名・会場名・大会種別（または形式）・メモ・（麻雀は対戦相手名も）を対象に部分一致検索。
- `sortPokerRecords(records, mode)` — `date-desc` / `date-asc` / `prize-desc`。`prize`は自由記述文字列のため先頭の数値部分だけ`parseFloat`で抽出してソート（数値が無いものは最下位）。
- `groupMahjongRecords(records, sortMode)` — 日付＋大会名でグループ化し、各グループ内は回戦番号順、グループ自体は`date-desc`/`date-asc`/`point-desc`/`point-asc`で並び替え。
- `setMjSort(mode)` / `setPkSort(mode)` — ソートモードを切り替えて再描画。

### 集計・描画
- `renderMahjong()` — 検索・グループ化・相性判定を反映して一覧描画、統計（記録数・平均順位・平均ポイント・トップ率・連対率）を更新。
- `renderPoker()` — 同様にポーカー一覧・統計（記録数・平均順位）を更新。
- `renderMembers()` / `renderVenues()` — メンバー・会場の一覧描画、チップ・セレクト要素への反映。
- `renderOpponentChips()` / `renderOpponentPointInputs()` / `renderVenueChips()` — フォーム内のチップ・入力欄の再描画。
- `opponent-stats-select` の `change` イベントハンドラ（無名関数、index.html:1018-1052） — 選択したメンバーとの対戦成績（対局数・平均順位・トップ率・連対率）を算出・表示。

### 入力検証
- `parseIntStrict(v, opts)` — 整数の厳密検証。空欄・NaN・"-"単独・小数・範囲外はすべてng。
- `parseFloatStrict(v, opts)` — 小数可の厳密検証。`allowEmpty`オプションで空欄をokとして扱える（ポーカーのバイイン欄で使用）。
- `sanitizeDecimalValue(v)` — ポイント入力欄の`oninput`用。全角数字・全角記号を半角に正規化し、数字・小数点1つ・先頭マイナスのみを許可。
- `toggleSignValue(v)` / `updateSignButtonState(btnEl, v)` / `toggleMjPointSign()` / `toggleOpponentPointSign(id)` — ±ボタンの符号反転とボタンの見た目（`sign-btn-active`）の同期。
- `toNum(v)` — **表示・合計計算用**。`parseFloat`結果がNaNなら0にフォールバック（空文字・"-"単独をエラー扱いしない）。
- `escapeHtml(str)` — `innerHTML`組み立て時のXSS対策。DOM要素の`textContent`経由でエスケープ。

---

## 5. 直すときの注意

- **migrate系は起動時に必ず走る**: `sanitizeLoadedData()` → `migrateDuplicateMemberIds()` → `migrateRecordFields()` の順で、スクリプト末尾（index.html:1645-1647）から必ず呼ばれる。新しいフィールドを追加するときは、既存の保存データに対する後方互換処理を `migrateRecordFields()` に足す運用になっている。ここを外すと、過去データがロードできなくなったり画面が壊れたりする。
- **`COMPAT_*` 定数の意味**（index.html:602-604）:
  - `COMPAT_MIN_MATCHES = 10` … 相性判定の対象にするための、その相手との最低同卓回戦数
  - `COMPAT_MIN_CANDIDATES = 10` … 相性判定そのものを発動させるための、判定対象人数の下限（これ未満なら誰も好相性/苦手として色付けしない）
  - `COMPAT_TOP_N = 5` … 好相性・苦手相性としてそれぞれ表示する上位人数
- **会場の削除ガードは未実装**: `deleteVenue(id, btn)`（index.html:948-968）は、使用中の記録件数を数えて`confirm()`で警告は出すが、キャンセルしなければそのまま削除される。削除後、該当記録の`venueId`は残ったまま（記録側のデータは書き換えられない）。表示側（`venues.find(v => v.id === r.venueId)`）はvenueが見つからなければ単に空扱いになるため実害は出ないが、**紐付けを外す・防ぐ処理そのものは存在しない**。
- **表示系は`toNum`、保存判定は`parseFloatStrict`/`parseIntStrict`という使い分け**:
  - `toNum(v)`（NaN→0フォールバック）は、リアルタイムの合計表示（`updatePointSum()`）など「見た目の途中経過」でのみ使う。
  - 実際に記録として保存する値の検証は `parseFloatStrict` / `parseIntStrict` を使い、NaNや範囲外はNGとしてフォームを弾く（`getCurrentScores()`, フォームのsubmitハンドラ内）。
  - この2系統を混同して保存側に`toNum`を使うと、本来弾くべき不正入力（空欄や"-"単独）がpoint=0として保存されてしまうので注意。
- **GitHubにアップし直さないと公開版は変わらない**: ローカルの`index.html`を編集しただけでは `https://worldmahjongunion-sys.github.io/mahjong-poker-record/` は更新されない。これまでの更新はすべてGitHubのWeb UIから直接ファイルをアップロードする形で行われてきており、ローカルのgit履歴はGitHub側と完全には同期していない（`git status`でローカルに未コミットの変更や未追跡ファイルが多数あるのはこのため）。ローカルからpushする前には必ず `git fetch origin` で差分を確認すること。当面はWeb UIからのアップロードで統一するのが安全。

---

## 6. 未着手の残タスク

- データのエクスポート/インポート（JSON）
  ※スマホとPCでlocalStorageが別なので記録を持ち運べない
- 会場の削除ガード
- 合計の赤字警告を「4人そろうまで出さない」に変更
