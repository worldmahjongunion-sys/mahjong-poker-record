# 麻雀・ポーカー成績記録 — 仕様書

最終更新: 2026-08-15（完成版）

---

## 1. 誰が使うか

自分と、一緒に打つ麻雀仲間が使う成績記録ツール。

もともとは自分ひとり用として作りはじめたが、公開URLを配って各自の端末で使ってもらう形にした。記録は端末ごとに独立しているため、他人の成績が自分に見えることはない。

---

## 2. 何を記録できるか

### 麻雀

- 日付
- 大会名（過去に入力した名前が候補として出る）
- 会場（あらかじめ登録した中からチップで選択）
- 大会種別（リーグ戦／ワンデー／セット／オンライン）
- 回戦番号
- 対戦相手（登録済みメンバーからチップで3人選択）
- 自分の最終ポイント（ウマオカ込み。マイナス・小数あり。例: -45.7）
- 対戦相手3人それぞれの最終ポイント
- メモ（役満・チョンボなど自由記述）

順位は入力しない。自分と対戦相手3人ぶんの最終ポイントを入れると、その場で自動的に順位が決まる（同点は同着として扱い、次の順位を飛ばす）。

入力中は4人の合計ポイントが表示される。供託が残った場合を除き通常は0になるので、合計が0以外のときは注意を促す表示が出る。

### ポーカー

- 日付
- 大会名（過去に入力した名前が候補として出る）
- 会場（麻雀と共通の登録リストから選択）
- 形式（MTT／SNG／リング）
- バイイン（未入力でも可）
- 参加人数
- 順位（参加人数を超える値は記録できない）
- プライズまたは得点ポイント（自由記述。金額とポイントで単位が混ざるため数値限定にしない）
- メモ（特定のハンドなど自由記述）

---

## 3. メンバー・会場の管理

「メンバー」タブでまとめて管理する。

- **メンバー**: 対戦相手の名前を登録・削除できる。登録したメンバーは麻雀の対戦相手選択に使う。
- **会場**: 麻雀・ポーカー共通のリスト。記録するときにチップで選ぶ。
- 使用中の会場を削除しようとすると、何件の記録で使われているかを示す確認メッセージが出る。ただし削除そのものは止まらない。削除すると、それまでの記録の会場表示は「未設定」扱いになる。

---

## 4. 画面と操作の流れ

- 画面上部に「麻雀」「ポーカー」「メンバー」のタブがあり、切り替えて使う。
- 麻雀・ポーカーの各タブに入力フォームと「記録する」ボタンがある。
- 記録すると下に一覧が表示される。麻雀は同じ日・同じ大会名の記録が自動でグループにまとまり、グループ内のポイント合計もあわせて表示される。
- 麻雀は記録すると回戦番号が自動で1つ繰り上がり、日付・大会名・対戦相手はそのまま残る。同じ大会の続きの回戦を続けて入力しやすい。
- 各記録には削除ボタンがある。
- キーワード検索で、大会名・会場・大会種別（ポーカーは形式）・メモ・対戦相手名から記録を絞り込める。絞り込むと上の集計も絞り込み後のデータで計算し直される。
- 並び替えができる。麻雀は新しい順／古い順／ポイントが高い順／低い順、ポーカーは新しい順／古い順／プライズが高い順。

### 入力しやすさのための工夫

- 最終ポイントの欄は、スマホでマイナスが打ちやすいようテキスト入力＋「±」ボタンで符号を反転する方式にしている。全角で入力しても半角に自動変換される。
- 空欄・変な値・長すぎる入力・ボタン連打をしても画面が落ちず、赤字で理由を表示する。
- 入力できる範囲: 麻雀の最終ポイントは -9999〜9999、ポーカーの参加人数は1〜9999人、順位は参加人数以下、バイインは0以上。大会名100文字・メモ500文字・名前50文字まで。

---

## 5. 集計・分析

- **麻雀タブ**: 記録数・平均順位・平均ポイント・トップ率・連対率
- **ポーカータブ**: 記録数・平均順位
- **メンバー別**: 登録メンバーを1人選ぶと、その相手と同卓したときの自分の対局数・平均順位・トップ率・連対率が見られる。
- **相性の色分け**: 十分な対戦数が貯まっているメンバーについて、自分との相性が良い相手・苦手な相手を自動で判定し、対戦相手を選ぶ画面で色分けする。対戦数や対象人数が少ないうちは判定しない。ラベルや凡例は出さず色のみ。

---

## 6. データの保存

- ブラウザのlocalStorageに保存する。ページを再読み込みしても記録は残る。
- localStorageは端末・ブラウザごとに独立しているため、スマホで記録したデータとPCで記録したデータは合わさらない。持ち運び・同期の仕組みは今のところない。
- 保存に失敗した場合（端末の保存容量がいっぱいなど）は画面上部に通知が出て、入力した内容は消えずに残る。
- 保存されていたデータ自体が壊れていた場合も、画面が真っ白になったりせず、空の状態で安全に起動して通知する。
- スマホのホーム画面にアイコン付きで追加できる（PWA対応）。

見た目は麻雀・ポーカーを連想させるグリーンのフェルト風背景＋ゴールドの配色。

---

## 7. 今回やらないこと

- 記念日カウントダウン機能
- 日々の気分・出来事の記録メモ機能
- グラフでの推移表示
- ポーカーのプライズ／得点ポイントの合計計算・収支管理（自由記述で金額とポイントの単位が混ざるため合算しない）
- **他の人との比較・共有機能（方針変更）**: 初版では「やらない」としていた。実際に作ったのは他人の成績を見たり共有したりする機能ではなく、自分から見て「その相手と対戦したときの自分の成績」を振り返る機能。対戦相手同士を比べる機能ではなく、記録を外部に共有する機能でもない。

---

## 8. 今後の候補

- **データのエクスポート／インポート（JSON）**: スマホとPCでlocalStorageが別のため、記録を持ち運べない。バックアップも兼ねられる。
- **会場の削除ガード**: 使用中の会場でも削除を止められない現状の改善。
- **合計の赤字警告を「4人そろうまで出さない」ように変更**: 現状は入力途中もずっと赤くなる。
