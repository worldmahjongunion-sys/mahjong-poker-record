# 仕様書：麻雀・ポーカー成績記録

## アプリ名
麻雀・ポーカー成績記録

## 誰が使う
自分だけが使う個人用の記録ツール

## 何を記録するか

### 麻雀
- 日付
- 大会名
- 順位（1〜4位）
- 最終ポイント（ウマオカ込み、マイナス値・小数あり。例：-45.7）

### ポーカー
- 日付
- 大会名
- 参加人数
- 順位
- プライズまたは得点ポイント（自由入力のテキスト欄。金額とポイントで単位が混ざるため、数値限定にはしない）

## どう動くか
- 画面上部に「麻雀」「ポーカー」のタブがあり、切り替えられる
- 各タブに入力フォームと「記録する」ボタンがある
- 記録すると、下に一覧が新しい順で表示される
- 各記録には削除ボタンがある
- 一覧の上に簡単な集計（記録数・平均順位）を表示する

## 入力欄で気をつける点（実装メモ）
- 麻雀の「最終ポイント」は `<input type="number" step="0.1">` にする。マイナス値・小数値を弾かないよう実装時に確認する
- ポーカーの「プライズまたは得点ポイント」は自由入力のテキスト欄にする（金額かポイントかは本人が分かる書き方で入力する想定。単位選択セレクトは今回は作らない）
- 大会名は `<datalist>` で過去に入力した大会名を候補表示できると入力が楽になる（余力があれば実装。必須ではない）

## 見た目
麻雀・ポーカーを連想させる雰囲気にする（例：グリーンのフェルト風背景、牌やトランプを思わせる配色・アイコン）

## データの保存
ブラウザのlocalStorageに保存する。ページを再読み込みしても、記録したデータが残っている。

## 今回はやらないこと
- 記念日カウントダウン機能
- 日々の気分・出来事の記録メモ機能
- 他の人との比較・共有機能
- プライズ／ポイントの合計計算・収支管理
- グラフでの推移表示
